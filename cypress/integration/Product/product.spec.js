describe('Product', () => {
  it('page loads with mocked content', () => {
    cy.log(
      'TODO: Investigate best tool for backend mocking and mock fetching product data. Candidates include: Mock Service Worker, JSON Server, Mirage.js, Service Mocker...',
    )
    expect(true).to.equal(true)
  })
})
