// import React from 'react'
// import { shallow } from 'enzyme'

// import CheckoutContainer from '../index'

describe('<CheckoutContainer />', () => {
  it('Expect to have unit tests specified', () => {
    expect(false).toEqual(false)
  })
})
