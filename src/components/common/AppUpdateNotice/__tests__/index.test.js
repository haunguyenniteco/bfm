// import React from 'react'
// import { shallow } from 'enzyme'

// import AppUpdateNotice from '../index'

describe('<AppUpdateNotice />', () => {
  it('Expect to have unit tests specified', () => {
    expect(false).toEqual(false)
  })
})
