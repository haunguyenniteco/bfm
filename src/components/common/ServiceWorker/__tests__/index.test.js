// import React from 'react'
// import { shallow } from 'enzyme'

// import ServiceWorkerProvider from '../index'

describe('<ServiceWorkerProvider />', () => {
  it('Expect to have unit tests specified', () => {
    expect(false).toEqual(false)
  })
})
